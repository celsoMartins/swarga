<%= form_for @new_vehicle, url: camping_group_vehicles_path(@camping_group) do |f| %>
    <%= render 'vehicle_fields', f: f %>

    <div class="row">
      <div class="col-xs-6">
        <%= f.submit t('general.buttons.save'), class: 'btn btn-success' %>
      </div>
    </div>
<% end %>
